services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=development
      - AI_SERVICE_URL=http://ai-service:5001
    volumes:
      - ./backend:/app
      - /app/node_modules
    depends_on:
      - ai-service
    command: npm run dev

  ai-service:
    build: ./ai-service
    ports:
      - "5001:5001"
    volumes:
      - ./ai-service:/app
    environment:
      - PYTHONUNBUFFERED=1